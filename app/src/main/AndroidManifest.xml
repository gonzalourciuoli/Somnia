<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.somnia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InfoValuations"></activity>
        <activity android:name=".AlarmsActivity" />
        <activity android:name=".Calculator" />
        <activity android:name=".ChangeEmail" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".Charts"/>
        <activity android:name=".Home"/>
        <activity android:name=".Info_alarmActivity" />
        <activity android:name=".logInActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".New_alarmActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".Settings" />
        <activity android:name=".Valuations" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>