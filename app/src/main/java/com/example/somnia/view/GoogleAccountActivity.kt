package com.example.somnia.view

import android.content.Intent
import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.Button
import com.example.somnia.R

class GoogleAccountActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        //setContentView(R.layout.activity_google_account)

        /*val login_google = findViewById<Button>(R.id.log_in_google)
        login_google.setOnClickListener {
            val intent = Intent(this@GoogleAccountActivity, FitnessActivity::class.java)
            startActivity(intent)
        }*/
    }
}
